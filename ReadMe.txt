cmdlogtime requires a command line text file that describes the input parameters.
See exampleCommandLine.txt

required packages
sys

Make sure you add the  path of wherever you have cmdlogtime.py (e.g. rms_pkgs/src) into your $PYTHONPATH .
